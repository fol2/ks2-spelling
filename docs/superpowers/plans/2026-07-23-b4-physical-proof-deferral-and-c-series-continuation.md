# B4 physical-proof deferral and C-series continuation record — 2026-07-23

The later `2026-07-23-b4-owner-go.md` supersedes this record's programme
decision and sequencing classification. The historical technical result and
deferred-proof boundaries below remain evidence of what was known when this
record was written.

This record lives outside
`2026-07-18-standalone-spelling-mobile-b4-capacitor-development-certification.md`
because that plan is a hash-frozen evidence input. It succeeds
`2026-07-19-b4-gate-b-physical-ios-record.md` for programme sequencing only.
It does not rewrite, invalidate or widen the historical B4 evidence.

## Unchanged technical result

Gate B Development remains `INCOMPLETE`, with
`technicalOutcome: investigation-required`. It is not relabelled `GO` or
`NO_GO`.

Historical physical iOS evidence remains valid for its exact application
checkpoint and toolchain. It records a complete physical journey, a settled
SQLite comparator and three attributed comparator breaches: first-install cold
launch, per-variant replay decode and answer-feedback publication. That evidence
is informative during continued development, but it is not evidence for the
future final release-candidate bytes.

The B4 performance-seams successor is
`c08775d2fdad9484b504b2658030872b7bae27bd`. Hosted exact-head run
[`29984999342`](https://github.com/fol2/ks2-spelling/actions/runs/29984999342)
passed Domain/Web, unsigned iOS Simulator compile and Android unsigned
debug/release compile. This is exact-head development evidence only: it is not
exact-main, physical-device, signed-distribution, store or final performance
evidence, and it does not change the B4 trichotomy.

## Owner sequencing decision

James, as programme owner, decided on 2026-07-23 to continue all non-release
product development on Capacitor before the next physical iPhone run. This is a
narrow sequencing exception to the earlier requirement for a B4 `GO` before
C-series work; it is not a technical gate pass.

Fresh physical iPhone, VoiceOver, acoustic, signed-distribution, store-download
and final performance proof are deferred until the product, visual system and
release-candidate bytes are frozen for Task 22. No historical or intermediate
physical capture may be reused as final-candidate evidence. Any application,
gateway, native, dependency or configuration change after that final checkpoint
invalidates the final distributions and restarts the affected Task 22 proof.

The existing Android decision is unchanged: no local physical Android device is
expected. Android real-device settlement remains deferred to Task 22 through a
separately approved hosted-real-device route that satisfies the existing
signed-install, store and evidence contracts.

## Obligations carried forward

Continued development must preserve these open obligations:

- adopt the UIScene lifecycle before claiming compatibility with an application
  built using the iOS 27 SDK;
- retain the attributed B4 cold-launch, audio-start and answer-feedback seams as
  final performance acceptance work;
- keep practice, profiles, learner state, Monster state and Camp state local and
  independent of Cloudflare or store availability;
- keep production code installed and bundled, with no `server.url`, remote HTML
  or remote JavaScript;
- complete a dedicated Visual / Theme / Asset Migration authority before C3
  child-facing production UI;
- create and verify the production signed Starter 20 content/audio package in
  C1, including complete 840-asset coverage and two approved British-English
  voices;
- implement production profile administration, Parent security and backup
  authority in C2; and
- bind every Task 22 capture to one fresh, exact final release-candidate
  checkpoint.

This record grants no authority to access signing material or to mutate a store
console, Cloudflare/R2 deployment or public release. Those actions remain behind
their explicit visible owner gates.

## Next bounded milestone

The first continued-development milestone is iOS UIScene adoption. Its evidence
sequence is:

1. retain the observed iOS 27 SDK launch failure and Apple's lifecycle
   requirement as the root-cause gate;
2. add one focused failing project contract to the existing iOS contract suite;
3. migrate the storyboard-hosted Capacitor window and app-owned plugin
   registration to a single-scene delegate without changing product behaviour;
4. prove the focused contract, unsigned Simulator compilation and existing
   native boundaries; and
5. obtain an independent review of the exact local candidate.

This milestone does not attempt a fresh physical iPhone run. After it closes,
continued work proceeds through C-series implementation slices with one bundled
C5 development-acceptance checkpoint. Visual / Theme / Asset Migration remains
the required implementation order before C3, but it is not a separate approval
gate.
