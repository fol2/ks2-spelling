# B4 owner GO — 2026-07-23

## Authority

James, as programme owner, approves **B4 `GO`** on 2026-07-23. This dated
record supersedes the programme decision and sequencing sections of
`2026-07-23-b4-physical-proof-deferral-and-c-series-continuation.md`. It does
not edit the hash-frozen B4 plan or rewrite historical evidence.

The effective decision is:

```text
programmeDecision: GO
platformDirection: Capacitor
historicalTechnicalOutcome: investigation-required
freshFinalPhysicalProof: deferred
```

## Basis of GO

- The bounded five-card local-first slice, A3 planner, SQLite transaction path,
  lifecycle recovery, offline audio path and iOS/Android development journeys
  were implemented and reviewed.
- Physical iOS execution completed the product journey and settled the isolated
  SQLite comparator.
- The remaining timing method measured a mixture of first-install system work,
  WebView/audio decode, bridge latency, test-runner polling and application
  work. It did not observe the intended application-owned comparator seams.
  Earlier isolation showed that measurement composition and virtual-device load
  materially changed the figures. The method therefore cannot prove those
  application thresholds and cannot demonstrate an application or
  disproportionate WebView ceiling.
- Exact-head successor `c08775d2fdad9484b504b2658030872b7bae27bd` passed hosted
  Domain/Web, unsigned iOS Simulator compile and Android unsigned debug/release
  compile in run
  [`29984999342`](https://github.com/fol2/ks2-spelling/actions/runs/29984999342).
- The recorded UIScene adoption gap was subsequently corrected in local
  C-series checkpoint `2fd0641`; this supports continued iOS development but
  does not retroactively alter the historical B4 device capture.

The owner therefore accepts Capacitor as the product platform and closes B4 as
`GO`. C-series work is authorised directly by this decision and no longer
depends on a sequencing exception.

## Evidence truth retained

This owner decision does not relabel a measured breach as a pass. Historical B4
reports continue to say `technicalOutcome: investigation-required` and retain
their raw cold-launch, slow-audio-start and answer-feedback observations. That
label describes the historical report, not a valid current verdict on the
application thresholds.

The external B4 timing method is rejected for application performance
acceptance because its observation points cannot separate the intended app seam
from system and harness time. Its apparent comparator breaches are therefore
not B4 blockers and must not be reused as final performance evidence.

Optimisation is deliberately later work. Before final performance acceptance, a
separate reviewed measurement authority must define observable app-owned start
and end points and prove that system, bridge and test-harness latency are not
being scored as application time. Until that exists, no pass or fail claim is
made for those three app thresholds.

This `GO` does not claim:

- fresh iOS 27 physical-device launch after UIScene adoption;
- final VoiceOver, TalkBack, acoustic or 200% text-scale device proof;
- signed iOS or Android distribution;
- App Store, Google Play, Cloudflare or R2 production truth; or
- final performance, store-download or release readiness.

Those claims remain behind the final Task 22/23 checkpoint and James's separate
visible approvals for credentials and external mutations.
